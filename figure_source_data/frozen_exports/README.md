# Frozen figure exports with corrected terminology

Figure2, Supplementary FigureS1 and Supplementary FigureS2 use verified label-only genotype terminology corrections. RESOLVED — label-only terminology correction completed; scientific data and geometry unchanged. The original five old labels remain documented in the correction manifest as historical provenance; no old-label export is retained as a current publication asset.

SVG changes are restricted to the five text strings and their textLength metrics. PDF content streams retain every drawing command and coordinate, replacing only those literal text strings. PNG (300 dpi) and TIFF (600 dpi) retain their original dimensions; changed pixels outside approved text regions are zero. Each format is edited against its own frozen original, preserving pre-existing format-specific page dimensions and rendering.

The companion GitHub package contains SVG/PNG reference copies for these three figures. The Dataset preserves SVG/PNG/PDF/TIFF. Figure3/Figure6 continue to use the previous label-fixed assets; Figure4 uses method_revision_v2. Other current figures and all numerical source tables remain unchanged.
