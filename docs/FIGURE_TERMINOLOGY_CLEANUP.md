# Figure terminology correction

The Figure2/FigureS1/FigureS2 publication terminology blocker is RESOLVED. Only the five previously identified graphic labels were changed to genotype/genotypes. All numerical source tables, scientific results, drawing geometry, axes, colors and panel layouts remain frozen; no scientific analysis or historical plotting script was executed.

SVG XML comparison permits only the specified text strings and their textLength metrics. PDF content-stream comparison permits only those literal text strings; all drawing commands and coordinates are unchanged. PNG/TIFF pixel comparisons show zero changed pixels outside the approved text rectangles, with dimensions and DPI preserved. Original formats are not regenerated from a different format.

See FIGURE_TERMINOLOGY_CORRECTION.tsv for logical original asset names and before/after SHA256 values. Current publication assets are under figure_source_data/frozen_exports/. Older source-code strings and data schema/file names are retained for provenance and are not final graphic labels. Final code/data licenses, creators and author release review remain pending.
